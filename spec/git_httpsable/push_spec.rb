require 'spec_helper'

describe GitHttpsable::Push do
  it 'has a version number' do
    expect(GitHttpsable::Push::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
